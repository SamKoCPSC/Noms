{
  "private": true,
  "scripts": {
    "tailwind:build": "npx @tailwindcss/cli -i tailwind.css -o assets/tailwind.css",
    "tailwind:watch": "npx @tailwindcss/cli -i tailwind.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0"
  }
}
